# IMPLEMENT FEATURE

Read `docs/AI_RULES.md`, `docs/GAME_VISION.md`, `docs/TECH_ARCHITECTURE.md`, `docs/NAMING_CONVENTIONS.md`, and any feature-specific docs such as `docs/GAMEPLAY_RULES.md`, `docs/UI_UX_RULES.md`, `docs/PERFORMANCE_RULES.md`, or `docs/SAVE_SYSTEM_RULES.md`.

Then:

- Restate the requested feature in project terms.
- List the existing systems, UI panels, and data structures that should be reused.
- Implement the feature with the smallest architecture change that fits the current codebase.
- Preserve current naming, save compatibility expectations, and input or UI patterns unless the task explicitly changes them.
- Verify the result with the project build or the closest available check.
